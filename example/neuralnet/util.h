#pragma once
#include "mshadow/tensor.h"

using namespace mshadow;


// simple function to load in mnist
inline void LoadMnist( const char *path_img, const char *path_label,
                       std::vector<int>& ylabel, TensorContainer<cpu,2>& xdata ){
    
    
}
